# Database host
# - Use "127.0.0.1" when running app locally
# - Use "db" when running app inside Docker (the service name in docker-compose)
DB_HOST=

# Database port
DB_PORT=

# Database name for the application
DB_NAME=

# Database username for authentication
DB_USER=

# Generate a secure password using: openssl rand -base64 32
DB_PASSWORD=
